# Plantilla de configuración para despliegue
# Copia este archivo a .env y ajusta los valores según tu entorno

# Región AWS donde desplegar
REGION=us-east-1

# Sufijo para nombres de recursos (ej: inventory-uploads-misufix, inventory-web-misufix)
# Úsalo con tu nombre o siglas para evitar conflictos
SUFFIX=misufix

# Email para suscripción SNS (notificaciones de stock bajo)
# Recibirás emails de confirmación en esta dirección
EMAIL=tu.email@example.com

# Perfil de AWS CLI a usar (por defecto 'default')
# Si tienes múltiples perfiles configurados, especifica el que tiene credenciales válidas
AWS_PROFILE=default

# Nombre de la tabla DynamoDB (no cambiar normalmente)
DDB_TABLE=Inventory
